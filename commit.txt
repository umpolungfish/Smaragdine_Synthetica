Mirror TimeFixedPoint and mark Section 4 (time as derived) PROVED

T = Work(T) as the least fixed point of the operad trace is now a theorem.
lean/TimeFixedPoint mirrors the proof from the buildable tree: Work is the
trace, its fixed points are exactly the Frobenius-special class,
time_least_fixed_point gives lfp_trace as the componentwise-least fixed point
over the twelve axis LinearOrders, and no_time_before_frobenius proves the
equation is unsolvable before G1 — time is derived, not primitive. The paper
skeleton Section 4 now records the theorem names to cite.
