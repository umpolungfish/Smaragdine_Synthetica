01_conventional_decomposition: CL8NK is not the summit any more

CLINK L9 was born 2026-07-13, so the frame of this document is stale in a way
that matters more than a missing row. The whole table is written from CL8NK as
the terminal layer, and L8's own catalog entry still calls itself terminal. It
is not: L9, the Gaussian Moat Resolution layer, sits above it at O_∞⁺.

The proximity labels were the quiet problem. apex / close / distant are the
navigator's distance from the L8 reference, not a ranking of the values, and
while L8 was the top nothing turned on the difference. Now several values this
table calls distant are apex in L9's register. Said so up front, next to the
provenance, because a reader who takes distant for low will draw exactly the
wrong conclusion from Ð.

Fixed the closing claims, which L9 falsifies as written. The apex column is
exactly what exceeds ZFC_fe on the road to L8; that qualifier is load bearing.
L9 exceeds L8 by RELINQUISHING both of L8's transcendence atoms: Γ gives up
BROADCAST for the three-unit stitch at the maximum gap of 1.0, and Ω gives up
BRAID for integer winding. Ð drops from the imscriptive fixed point to the value
this table reads as a 0-dimensional finite set, which L9 names PRIME_POINT, a
point-like prime atom, which is exactly what a Gaussian prime is. Four axes hold
fixed across the rung and they are the ones that matter: ƒ, ⊙, H, S. The
self-modeling criticality and the eternal Frobenius fixed point are precisely
what L9 does not give up. So the Frobenius conditions carry across dialects and
the transcendence atoms do not, which is the real content of the apex column.

Also stopped the document arguing with itself about H. It gave the axis meaning
as "temporal-memory / mirror asymmetry" and then two lines later said "H is
chirality, not temporal depth." H is chirality throughout. Rewrote the axis
meaning as handedness under mirror and shift and cleaned the two value readings
that still leaned on temporal memory. Left the sequencing axiom alone: temporal
directedness is correct there, that one really is about causal order.
