Mirror the proved GateOrdering and mark the ordering PROVED in the skeleton

The gate ordering 4 < 9 < 12 the paper thesis rests on is now a theorem, not
an assertion. lean/GateOrdering.lean mirrors the module I proved in the
buildable p4ramill tree: four operad layers, the G2 trace built on the
delta_C/mu_C Frobenius pair, trace_closes_iff_special as the hinge,
traced_needs_frobenius giving 4 < 9, the strict precedence chain with explicit
witnesses (w_frob, w_traced) proving no inclusion collapses, and
gate_ordering_theorem as the headline. Zero sorries, no native_decide.

The paper skeleton section that listed this as work-to-do now records it as
done with the theorem names to cite in Section 3, and resolves the earlier
Protection-irrelevant-to-O_inf mismatch by having G3 act on the traced operad
(winding plus O_inf on top of a closing trace) rather than reading it off the
bare tier function.
