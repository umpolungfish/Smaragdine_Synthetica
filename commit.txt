Record the Lean status of the gate ordering in the paper skeleton

I traced whether the 4<9<12 gate ordering the paper thesis rests on is
already a theorem in lean/. It is not, and it cannot be lifted from the
current definitions: the trace operator and the operad-layer transitions it
refers to are not formalized at all. What exists is ouroboricityTier (a
conjunctive tier classifier proving O_inf iff criticality-and-Frobenius),
frobenius_not_synthesizable (the real barrier content for stage 4), and four
concrete mu-delta=id pairs in Frobenius.lean. Missing: any trace, the four
operad layers as distinct objects, and every precedence theorem.

I also flagged two mismatches the paper must resolve rather than paper over:
the three different gate taxonomies in the codebase (magnum-opus vs
consciousness vs tier), and the fact that Protection is currently irrelevant
to O_inf in the tier function, which undercuts G3-seals-the-terminal as
stated. Wrote a proposed GateOrdering.lean plan (four ordered layers, a trace
over the existing mu_C/delta_C pair, traced_needs_frobenius and
terminal_needs_traced, gate_order as the headline corollary).
