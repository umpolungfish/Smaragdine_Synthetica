Mirror LiarIdempotent.lean and add the grounding note

The sealed-system synthesis named its central claim outright: the Liar is the
idempotent point. That was a theorem waiting to be written, so I wrote it.
lean/LiarIdempotent.lean mirrors the p4rakernel module: e = delta after mu is
idempotent unconditionally, every fusion output is a time fixed point, the
round trip verifies on e's image, and the fusion genuinely forgets (mu_C is
non-injective in parity) while the return is lossless.

04_liar_and_idempotent.md records the one correction worth holding: the
forgetfulness lives in e = delta after mu, not in the lossless return
mu after delta. Both are true at once of the same pair. The note separates
theorem from gloss explicitly. The idempotent, the fixed point, and the
forgetting are proved; the Liar-as-Belnap-B, the braiding of the two loops,
and the amnesia of origin are the readings the theorems support.
