Rename the CLI entry point ss → smaragdine, and guard the optional engine import

ss shadowed /usr/bin/ss; smaragdine is unambiguous. __init__ eagerly imported
a session module the emerald-tablet engine does not ship, breaking the import
though the CLI never uses it — now optional, sys.path shim dropped.
